#!/bin/bash

set -e

if [ "$EUID" -ne 0 ]; then
    echo "You need to be root to apply the puppet configuration, aborting."
    exit 1
fi

ROOTDIR="/etc/puppet/code/environments/production/"
cd "$ROOTDIR"

puppet apply "${ROOTDIR}/manifests/site.pp"
